import React from "react";

const Banner = () => {
  return (
    <div className="py-3 flex justify-center items-center bg-red-400 text-white font-tertiary">
      Notamos que no has iniciado sesión, por lo cual no podrás comprar productos.
    </div>
  );
};

export default Banner;
